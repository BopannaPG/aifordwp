Resolved. Cause: Floor 3 DHCP scope continued to distribute a decommissioned DNS server after the overnight DNS migration, so affected Windows 11 clients could not resolve/reach domain controller services, causing Group Policy failure and black screen symptoms. Action: Corrected DNS assignment for affected Floor 3 clients to the new DNS server (10.10.0.10) and re-verified Group Policy processing health; resolution was verified at 07:40:05 AM with no further issues reported. Preventive: Add mandatory DHCP DNS option update and post-change lease plus DC-resolution plus Group Policy validation checks before and after DNS decommission changes. User confirmed working.